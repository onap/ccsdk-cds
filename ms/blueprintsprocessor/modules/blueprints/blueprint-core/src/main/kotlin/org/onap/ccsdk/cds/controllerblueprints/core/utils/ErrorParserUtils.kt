package org.onap.ccsdk.cds.controllerblueprints.core.utils

object ErrorParserUtils {
        /** parse error message and get action  */
        fun getAction(message: String): String {
            return ""
        }

        /** parse error message and get cause  */
        fun getCause(message: String): String {
            return ""
        }
}
